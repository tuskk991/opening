<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>https://tusk1991.com</title>
    <style>
        :root {
            /* 主色系：鮮艷群青底板 x 桃紅文字 */
            --c1: #FF3385; /* 霓虹桃紅（標題、高亮文字、主要內文） */
            --c2: #FF007F; /* 電子病嬌粉（重點高亮、警告、按鈕） */
            --c3: #FF80BF; /* 淡桃紅（次要輔助字體） */
            --c4: #000088; /* 視窗標題欄深群青 */
            
            /* 群青色大格子與底板 */
            --bg: #03050A; /* 宇宙極黑背景 */
            --win-bg: rgba(0, 0, 198, 0.92); /* 半透明群青色大格子底板 (#0000C6) */
            --win-bg-solid: #0000C6; /* 純群青色底板 (#0000C6) */
            --border-light: #3333FF; /* 群青邊框發光線 */
            
            /* 文字顏色：全螢光桃紅 */
            --text-main: #FF3385; /* 桃紅內文 */
            --text-muted: #FF80BF; /* 淡桃紅系統文字 */
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            /* 套用加粗仿宋體 */
            font-family: "FangSong", "仿宋", "FangSong_GB2312", "STFangsong", serif;
            font-weight: bold;
        }

        body {
            background-color: var(--bg);
            background-image: url('bg.jpg'), linear-gradient(135deg, #02000a 0%, #0d001a 50%, #000015 100%);
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            background-repeat: no-repeat;
            color: var(--text-main);
            display: flex;
            justify-content: center;
            align-items: flex-start;
            min-height: 100vh;
            padding: 20px 10px;
            overflow-x: hidden;
            position: relative;
        }

        /* --- CRT 螢幕掃描線濾鏡 (Scanline Effect) --- */
        body::before {
            content: " ";
            display: block;
            position: fixed;
            top: 0; left: 0; bottom: 0; right: 0;
            background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.35) 50%);
            background-size: 100% 4px;
            z-index: 9999;
            pointer-events: none;
            opacity: 0.6;
        }

        /* --- POPUP 視窗 --- */
        #modal-overlay {
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            background: rgba(3, 5, 10, 0.88);
            backdrop-filter: blur(6px);
            z-index: 999;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .win95-pop {
            width: 320px;
            background: var(--win-bg-solid);
            border: 1px solid var(--c1);
            box-shadow: 4px 4px 0px #000, 0 0 15px rgba(255, 51, 133, 0.3);
            padding: 3px;
        }

        .win95-title {
            background: var(--c4);
            color: var(--c1);
            padding: 4px 8px;
            font-size: 0.85rem;
            display: flex;
            justify-content: space-between;
            border-bottom: 1px solid var(--c1);
            letter-spacing: 1px;
        }

        .win95-body {
            padding: 20px 15px;
            text-align: center;
        }

        .win95-body label {
            display: block;
            margin-bottom: 12px;
            color: var(--c1);
            font-size: 0.95rem;
            letter-spacing: 1px;
        }

        .win95-input {
            width: 100%;
            background: #000066;
            border: 1px solid var(--c1);
            color: var(--c1);
            padding: 8px;
            margin-bottom: 15px;
            outline: none;
            text-align: center;
        }

        .win95-input:focus {
            border-color: var(--c2);
            box-shadow: 0 0 8px rgba(255, 0, 127, 0.5);
        }

        .win95-btn {
            background: var(--c2);
            border: 1px solid var(--c1);
            color: #fff;
            padding: 6px 20px;
            cursor: pointer;
            box-shadow: 2px 2px 0px #000;
        }

        .win95-btn:hover {
            background: var(--c1);
            color: #000;
            box-shadow: 1px 1px 0px #000;
        }

        /* --- 信封彈窗 (Envelope Modal) --- */
        #envelope-overlay {
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            background: rgba(3, 5, 10, 0.9);
            backdrop-filter: blur(8px);
            z-index: 1000;
            display: none;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .envelope-win {
            background: var(--win-bg-solid);
            border: 1px solid var(--c1);
            box-shadow: 4px 4px 0px #000, 0 0 20px rgba(255, 51, 133, 0.3);
            max-width: 550px;
            width: 100%;
            padding: 4px;
            animation: popIn 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        @keyframes popIn {
            from { transform: scale(0.8); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }

        .envelope-body {
            padding: 10px;
            text-align: center;
        }

        .envelope-body img {
            width: 100%;
            height: auto;
            border: 1px solid var(--border-light);
            display: block;
        }

        /* --- 主容器布局 --- */
        .main-screen {
            width: 100%;
            max-width: 920px;
            display: flex;
            flex-direction: column;
            gap: 20px;
            opacity: 0.2;
            pointer-events: none;
            transition: opacity 0.5s;
        }

        .main-screen.unlocked {
            opacity: 1;
            pointer-events: auto;
        }

        /* 頂部 URL 與 Banner */
        .url-bar {
            background: var(--win-bg-solid);
            border: 1px solid var(--c1);
            padding: 6px 10px;
            font-size: 0.8rem;
            color: var(--c1);
            text-align: center;
            box-shadow: inset 0 0 8px rgba(0, 0, 0, 0.8);
            letter-spacing: 1px;
        }

        .banner-box {
            width: 100%;
            min-height: 120px;
            max-height: 200px;
            border: 1px solid var(--c1);
            background: var(--win-bg-solid);
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            position: relative;
            box-shadow: 0 0 12px rgba(255, 51, 133, 0.3);
        }

        .banner-box img {
            width: 100%;
            height: 100%;
            display: block;
            object-fit: cover;
            object-position: left 30%;
        }

        /* 雙欄核心排版 */
        .content-columns {
            display: flex;
            gap: 20px;
            align-items: flex-start;
        }

        .left-col {
            flex: 1.4;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .right-col {
            flex: 0.8;
            border: 1px solid var(--c1);
            background: var(--win-bg);
            padding: 15px;
            display: flex;
            flex-direction: column;
            gap: 10px;
            box-shadow: 4px 4px 0px #000, 0 0 12px rgba(255, 51, 133, 0.2);
            backdrop-filter: blur(4px);
        }

        /* 群青色 Profile 大格子 */
        .profile-container {
            border: 1px solid var(--c1);
            background: var(--win-bg);
            padding: 15px;
            display: flex;
            gap: 15px;
            position: relative;
            box-shadow: 4px 4px 0px #000, 0 0 12px rgba(255, 51, 133, 0.2);
            backdrop-filter: blur(4px);
        }

        .pfp-box {
            width: 85px;
            height: 85px;
            border: 1px solid var(--c1);
            background: #000044;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 0.8rem;
            color: var(--c3);
            flex-shrink: 0;
            overflow: hidden;
        }

        .pfp-box img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .profile-info {
            flex-grow: 1;
        }

        .profile-info h2 {
            font-size: 1.3rem;
            color: var(--c1);
            margin-bottom: 2px;
            letter-spacing: 1px;
            text-shadow: 0 0 8px rgba(255, 51, 133, 0.6);
        }

        .sub-name {
            font-size: 0.72rem;
            color: var(--c2);
            margin-bottom: 8px;
            letter-spacing: 0.5px;
        }

        .profile-tags {
            display: flex;
            flex-wrap: wrap;
            gap: 5px;
            margin-bottom: 12px;
        }

        .info-tag {
            font-size: 0.7rem;
            background: var(--c2);
            color: #fff;
            padding: 1px 6px;
        }

        .profile-desc {
            font-size: 0.85rem;
            color: var(--text-main);
            line-height: 1.6;
        }

        .profile-desc p {
            margin-bottom: 8px;
        }

        .profile-desc p:last-child {
            margin-bottom: 0;
        }

        .system-log {
            font-size: 0.7rem;
            color: var(--text-muted);
            margin-top: 12px;
            padding-top: 6px;
            border-top: 1px dashed var(--border-light);
            font-style: italic;
            display: flex;
            flex-direction: column;
            gap: 4px;
        }

        /* 標籤群組區塊（增加群青底板） */
        .tag-box {
            border: 1px solid var(--c1);
            background: var(--win-bg);
            padding: 15px;
            box-shadow: 4px 4px 0px #000, 0 0 12px rgba(255, 51, 133, 0.2);
            backdrop-filter: blur(4px);
        }

        .section-title {
            font-size: 0.9rem;
            color: var(--c2);
            border-bottom: 1px solid var(--border-light);
            padding-bottom: 4px;
            margin-bottom: 10px;
            letter-spacing: 1px;
        }

        .tag-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
        }

        .kv-tag {
            border: 1px solid var(--c1);
            background: rgba(255, 51, 133, 0.2);
            color: var(--c1);
            padding: 4px 8px;
            font-size: 0.78rem;
            box-shadow: 0 0 5px rgba(255, 51, 133, 0.2);
        }

        .vibe-tag {
            border: 1px solid var(--c2);
            background: rgba(255, 0, 127, 0.25);
            color: #FF80BF;
            padding: 4px 8px;
            font-size: 0.78rem;
            box-shadow: 0 0 5px rgba(255, 0, 127, 0.3);
        }

        /* Don't 群青色區塊 */
        .dont-box {
            border: 1px solid var(--c2);
            background: var(--win-bg);
            padding: 15px;
            box-shadow: 4px 4px 0px #000, 0 0 10px rgba(255, 0, 127, 0.3);
        }

        .dont-box h3 {
            color: var(--c2);
            font-size: 0.95rem;
            margin-bottom: 10px;
            letter-spacing: 2px;
        }

        .dont-box ul {
            list-style: none;
            font-size: 0.82rem;
            color: var(--c1);
        }

        .dont-box li {
            margin-bottom: 8px;
            padding-left: 15px;
            position: relative;
        }

        .dont-box li::before {
            content: "✕";
            position: absolute;
            left: 0;
            color: var(--c2);
        }

        /* START 按鈕 */
        .start-section {
            text-align: center;
            padding: 25px 0 10px 0;
            border-top: 1px dashed var(--border-light);
        }

        .ready-text {
            font-size: 1.1rem;
            color: var(--c1);
            margin-bottom: 15px;
            letter-spacing: 2px;
        }

        .start-btn {
            background: var(--c2);
            color: #fff;
            border: 2px solid var(--c1);
            font-size: 1.4rem;
            padding: 10px 35px;
            cursor: pointer;
            letter-spacing: 4px;
            box-shadow: 4px 4px 0px #000, 0 0 15px rgba(255, 0, 127, 0.5);
            transition: all 0.1s;
            text-shadow: 0 0 5px #000;
        }

        .start-btn:hover {
            background: var(--c1);
            color: #000;
            text-shadow: none;
            transform: translate(-2px, -2px);
            box-shadow: 2px 2px 0px #000, 0 0 20px rgba(255, 51, 133, 0.9);
        }

        /* 右欄：坑清單 */
        .fandom-list {
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .fandom-item {
            display: block;
            background: #000088;
            border: 1px solid var(--border-light);
            color: var(--c1);
            padding: 6px 10px;
            font-size: 0.8rem;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.15s ease;
            position: relative;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .fandom-item:hover {
            background: var(--c2);
            color: #fff;
            border-color: #fff;
            padding-left: 14px;
            box-shadow: -4px 0 0 var(--c1), 0 0 10px rgba(255, 0, 127, 0.6);
        }

        /* 信封按鈕樣式 */
        .envelope-item {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            background: rgba(255, 51, 133, 0.08);
            border: 1px dashed var(--c1);
            color: var(--c1);
            padding: 8px;
            font-size: 0.85rem;
            margin-top: 10px;
            cursor: pointer;
            transition: all 0.2s ease;
            letter-spacing: 1px;
        }

        .envelope-item:hover {
            background: var(--c1);
            color: #000;
            border-style: solid;
            box-shadow: 0 0 15px rgba(255, 51, 133, 0.8);
        }

        /* 浮動說明框 (Tooltip) */
        #custom-tooltip {
            position: fixed;
            display: none;
            background: var(--win-bg-solid);
            border: 1px solid var(--c1);
            color: var(--c1);
            padding: 8px 12px;
            font-size: 0.78rem;
            max-width: 280px;
            box-shadow: 4px 4px 0px #000, 0 0 10px rgba(255, 51, 133, 0.3);
            z-index: 900;
            pointer-events: none;
            line-height: 1.4;
            white-space: pre-line;
        }

        /* GALGAME 畫面 */
        #galgame-screen {
            display: none;
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            background: rgba(3, 5, 12, 0.94);
            z-index: 1001;
            flex-direction: column;
            justify-content: flex-end;
            align-items: center;
            padding: 20px;
            background-image: 
                linear-gradient(rgba(255, 51, 133, 0.05) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255, 51, 133, 0.05) 1px, transparent 1px);
            background-size: 20px 20px;
        }

        .gal-dialog-box {
            width: 100%;
            max-width: 680px;
            background: var(--win-bg-solid);
            border: 1px solid var(--c1);
            padding: 20px;
            box-shadow: 0 0 20px rgba(255, 51, 133, 0.4);
            position: relative;
            cursor: pointer;
        }

        .gal-name {
            position: absolute;
            top: -16px; left: 20px;
            background: var(--c2);
            color: #fff;
            padding: 2px 12px;
            font-size: 0.9rem;
            border: 1px solid #fff;
        }

        .gal-text {
            color: var(--c1);
            font-size: 1rem;
            line-height: 1.6;
            margin-top: 10px;
            min-height: 60px;
        }

        .gal-next-hint {
            text-align: right;
            font-size: 0.8rem;
            color: var(--c2);
            animation: blink 1s infinite;
        }

        @keyframes blink { 50% { opacity: 0; } }

        /* 響應式排版 */
        @media (max-width: 768px) {
            .content-columns {
                flex-direction: column;
            }
            .right-col {
                width: 100%;
            }
        }
    </style>
</head>
<body>

    <!-- 全域浮動提示框 -->
    <div id="custom-tooltip"></div>

    <!-- 1. POPUP 問候彈窗 -->
    <div id="modal-overlay">
        <div class="win95-pop">
            <div class="win95-title">
                <span>SYSTEM_CHECK.EXE</span>
                <span>[X]</span>
            </div>
            <div class="win95-body">
                <label for="visitor-input">「你是為何來到這裡的？」</label>
                <input type="text" id="visitor-input" class="win95-input" placeholder="請在此輸入回答..." autofocus>
                <button class="win95-btn" onclick="submitAnswer()">[ 送出 / ENTER ]</button>
            </div>
        </div>
    </div>

    <!-- 可點開的神秘信封彈窗 -->
    <div id="envelope-overlay">
        <div class="envelope-win">
            <div class="win95-title">
                <span>淵のオタクソング9選.JPG</span>
                <span style="cursor: pointer;" onclick="closeEnvelope()">[X]</span>
            </div>
            <div class="envelope-body">
                <img src="song9.jpg" alt="淵のオタクソング9選" onerror="this.alt='［ 請確認圖片檔名是否為 song9.jpg ］'">
            </div>
        </div>
    </div>

    <!-- 2. 主頁層 -->
    <div class="main-screen" id="mainScreen">
        
        <div class="url-bar">https://tusk1991.com</div>

        <!-- 橫幅區塊 -->
        <div class="banner-box">
            <img src="banner.jpg" alt="［ 橫 幅 B A N N E R ］" onerror="this.style.display='none'; this.nextElementSibling.style.display='inline';">
            <span class="banner-placeholder" style="display:none;">［ 請確認橫幅圖檔名是否為 banner.jpg ］</span>
        </div>

        <!-- 雙欄架構 -->
        <div class="content-columns">
            
            <!-- 左欄：Profile 與主資訊 -->
            <div class="left-col">
                <div class="profile-container">
                    <div class="pfp-box">
                        <img src="avatar.jpg" alt="[ PFP ]" onerror="this.style.display='none'; this.parentElement.innerText='[ PFP ]';">
                    </div>
                    <div class="profile-info">
                        <h2>@tuskk991</h2>
                        <div class="sub-name">我叫久淵 我沒有其他名字不用猶豫</div>
                        
                        <div class="profile-tags">
                            <span class="info-tag">GMT+8</span>
                            <span class="info-tag">INFP</span>
                            <span class="info-tag">ANGEL</span>
                            <span class="info-tag">He/Him</span>
                        </div>

                        <div class="profile-desc">
                            <p>你好，你好。無論你是因為什麼原因來到這裡的，我誠摯地歡迎你。</p>
                            <p>你可能不小心跌進了某人設下的落穴，或者只是開錯了門扉。</p>
                            <p>天哪 是誰那麼狠心對你設下陷阱呢?</p>
                            <p>不過，這裡只是一個存在於虛無書庫中的小型資料庫喔。</p>
                            <p>角落的東西正在抽搐，不過可以不用理他。</p>
                            <p>別擔心，這裡沒有時間的概念，也許有出口——隨意逛逛吧。</p>
                        </div>

                        <div class="system-log">
                            <div id="visitorLog">> 訪客動機紀錄：[尚未記錄]</div>
                            <div id="visitTimer">> 停留時間：0 秒</div>
                        </div>
                    </div>
                </div>

                <!-- Keywords（群青色底板） -->
                <div class="tag-box">
                    <div class="section-title">// KEYWORDS</div>
                    <div class="tag-grid">
                        <span class="kv-tag">破破爛爛但真心</span>
                        <span class="kv-tag">感官重度依賴</span>
                        <span class="kv-tag">體驗 > 規則</span>
                        <span class="kv-tag">極端體驗型</span>
                        <span class="kv-tag">手帳 / 剪貼生活</span>
                    </div>
                </div>

                <!-- Vibes（群青色底板） -->
                <div class="tag-box">
                    <div class="section-title">// VIBES</div>
                    <div class="tag-grid">
                        <span class="vibe-tag">天使界隈</span>
                        <span class="vibe-tag">水色系統</span>
                        <span class="vibe-tag">地雷系</span>
                        <span class="vibe-tag">kawaiigore</span>
                        <span class="vibe-tag">gore</span>
                        <span class="vibe-tag">龐克 / 鉚釘</span>
                        <span class="vibe-tag">碎核 / 傷核</span>
                        <span class="vibe-tag">Liminal Space</span>
                        <span class="vibe-tag">Analog Horror</span>
                    </div>
                </div>

                <!-- Don't -->
                <div class="dont-box">
                    <h3>DON'T :</h3>
                    <ul>
                        <li><strong>推坑／安利米哈遊相關內容：</strong>我不喜歡米哈遊，請不要對我貼臉相關資訊。</li>
                        <li>在還沒認識彼此就開過度熟識的玩笑。</li>
                        <li>傳給我屌照 / 竹節蟲 / 大拇指等奇怪的東西。</li>
                    </ul>
                </div>

                <!-- Ready & Start -->
                <div class="start-section">
                    <div class="ready-text">Ready ?</div>
                    <button class="start-btn" onclick="startGame()">[ START ]</button>
                </div>
            </div>

            <!-- 右欄：坑清單 -->
            <div class="right-col">
                <div class="section-title">// FANDOMS & INTERESTS</div>
                <div class="fandom-list">
                    <div class="fandom-item" data-tooltip="chunithm / maimai 都有摸（比較常打中二，偶爾摸女武神跟太鼓達人）&#10;プロセカ：主推奏、25時團推&#10;Phigros / Cytus 1&2（剛重載回來重打進度）&#10;還有很多想不起來先這樣！">音遊</div>
                    <div class="fandom-item" data-tooltip="超級超級爵銀龍激推！喜歡爵銀水仙&#10;World / Rise / Wilds / Now 都涉獵&#10;歡迎來約打！貓叫麻葉&#10;武器：弓 / 蟲棍&#10;近期待在新大陸忙著重建虛黑城">魔物獵人</div>
                    <div class="fandom-item" data-tooltip="劇情全補完，不用擔心暴雷！&#10;吃女同 & JayVic（左右位可換）&#10;但我沒打 LOL，不想死媽。">奧術</div>
                    <div class="fandom-item" data-tooltip="體裁包含原創世界觀、設定集中頁面">原創</div>
                    <div class="fandom-item" data-tooltip="其實進度沒玩很多，也不是打牌好手，偶爾會上線吧～">巫師3 (蹭我朋友的遊戲庫)</div>
                    <div class="fandom-item" data-tooltip="天空之劍 ｜ 曠野之息 ｜ 王國之淚 ｜ 災厄啟示錄&#10;CP 都吃歡迎安利我！&#10;喜歡加儂時林 / 林克水仙">薩爾達系列</div>
                    <div class="fandom-item" data-tooltip="我不吃 madoponi 和 mado X masada 愛情向，&#10;喜歡モノ姐妹。">夢日記 / ゆめにっき / ゆめ2っき</div>
                    <div class="fandom-item" data-tooltip="近期中毒遊玩地雷女模擬器。">主播女孩重度依賴</div>
                    <div class="fandom-item" data-tooltip="濃度很高！主推凜&#10;all明都吃，也喜歡處刑人組和式凜&#10;除了怪怪拉郎以外都吃">咎狗之血</div>
                    <div class="fandom-item" data-tooltip="主推諾伊茲&#10;CP 喜歡諾蒼和雀椅&#10;劇情有生之年會補完">戲劇性謀殺 (DMMd)</div>
                    <div class="fandom-item" data-tooltip="特別特別喜歡 G 向的部分，二創圖也會有&#10;雷者請自行避雷！">沙耶之歌</div>
                    <div class="fandom-item" data-tooltip="王子季入坑，表演季無斷至梵谷季&#10;出沒時間在饅頭季前後&#10;有養一隻飛行季出生的無翼，多半在掛機養老&#10;互火/好友隨時歡迎，請主動丟代碼 (備註隨意)">光遇</div>
                    <div class="fandom-item" data-tooltip="雖然很少上線了，但很喜歡這樣消磨時間">星露谷物語</div>
                    <div class="fandom-item" data-tooltip="主推紅傘！&#10;不喜歡乙女腐">文字化化</div>
                    <div class="fandom-item" data-tooltip="近期有看完的新番！&#10;喜歡動畫的風格與運鏡，原畫也是屌得沒話說">坂本日常</div>
                    <div class="fandom-item" data-tooltip="動畫看一半、遊戲摸一點（卡在第二季一半）&#10;主推 Yumoto">美男高校地球防衛部LOVE</div>
                    <div class="fandom-item" data-tooltip="死盤子！會在場次收周邊的程度">Dead Plate</div>
                    <div class="fandom-item" data-tooltip="就是那種一下有一坨人上線熬夜爆肝，&#10;一下大家都消失的遊戲（近期處於後者）">MC</div>
                    <div class="fandom-item" data-tooltip="大部分 P 主都聽聽&#10;咪哭一生推！">Vocaloid (初音未來 / 鏡音雙子等)</div>
                    <div class="fandom-item" data-tooltip="小資歷調查員&#10;只跑過毒湯、故鄉幻影、朋友自己寫的本">TRPG (跑團)</div>
                    <div class="fandom-item" data-tooltip="這個人太值得我自己開一篇來講了！&#10;無論遊戲、3D 還是音樂，這傢伙都是個天才！&#10;超愛 GO GOME 和夜詛&#10;目前正在瘋狂循環【ミッドノクターン】和【はい、そうです。】">市松寿ゞ謡</div>
                </div>

                <!-- 點開的信封按鈕 -->
                <div class="envelope-item" onclick="openEnvelope()" data-tooltip="點擊開啟：淵のオタクソング9選">
                    ✉ [ SECRET ENVELOPE ]
                </div>
            </div>

        </div>
    </div>

    <!-- 3. GALGAME 遊戲對話框畫面 -->
    <div id="galgame-screen">
        <div class="gal-dialog-box">
            <div class="gal-name">SYSTEM</div>
            <div class="gal-text" id="galText">>>> 系統啟動中...</div>
            <div class="gal-next-hint">▼ 點擊繼續...</div>
        </div>
    </div>

    <script>
        let secondsPassed = 0;
        let timerInterval = null;

        function playSFX(type) {}
        function playBGM() {}

        // 1. 處理 Pop-up 輸入與開始計時
        function submitAnswer() {
            playSFX('click');
            const input = document.getElementById('visitor-input').value.trim();
            const logDisplay = document.getElementById('visitorLog');
            const overlay = document.getElementById('modal-overlay');
            const main = document.getElementById('mainScreen');

            if(input !== "") {
                logDisplay.innerText = `> 訪客動機紀錄：「${input}」`;
            } else {
                logDisplay.innerText = `> 訪客動機紀錄：[未輸入 - 迷路的訪客]`;
            }

            overlay.style.display = 'none';
            main.classList.add('unlocked');

            startTimer();
            playBGM();
        }

        function startTimer() {
            if (timerInterval) return;
            const timerDisplay = document.getElementById('visitTimer');
            timerInterval = setInterval(() => {
                secondsPassed++;
                timerDisplay.innerText = `> 停留時間：${secondsPassed} 秒`;
            }, 1000);
        }

        document.getElementById('visitor-input').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                submitAnswer();
            }
        });

        // 2. 自訂 Hover Tooltip
        const tooltip = document.getElementById('custom-tooltip');
        const fandomItems = document.querySelectorAll('.fandom-item, .envelope-item');

        fandomItems.forEach(item => {
            item.addEventListener('mouseenter', (e) => {
                const text = item.getAttribute('data-tooltip');
                if(text) {
                    tooltip.innerText = text;
                    tooltip.style.display = 'block';
                }
            });

            item.addEventListener('mousemove', (e) => {
                tooltip.style.left = (e.clientX + 15) + 'px';
                tooltip.style.top = (e.clientY + 15) + 'px';
            });

            item.addEventListener('mouseleave', () => {
                tooltip.style.display = 'none';
            });
        });

        // 3. 信封 Modal 開關邏輯
        function openEnvelope() {
            playSFX('click');
            document.getElementById('envelope-overlay').style.display = 'flex';
        }

        function closeEnvelope() {
            playSFX('click');
            document.getElementById('envelope-overlay').style.display = 'none';
        }

        document.getElementById('envelope-overlay').addEventListener('click', function(e) {
            if (e.target === this) {
                closeEnvelope();
            }
        });

        // 4. Galgame 遊戲邏輯與打字機效果
        const dialogs = [
            "「警報：未知的使用者存取了這段記憶鏈結。」",
            "「正在進行讀取... 請注意，眼前看到的畫面可能包含大量的電波與分神碎屑。」",
            "「既然你已經來到了這裡，就請慢慢探索吧。」",
            "「祝你有個愉快的一天。遊戲... 即將開始。」"
        ];
        
        let currentStep = 0;
        let typingTimer = null;
        let isTyping = false;

        function startGame() {
            playSFX('click');
            document.getElementById('galgame-screen').style.display = 'flex';
            currentStep = 0;
            showDialog();
        }

        function showDialog() {
            const galText = document.getElementById('galText');
            if(currentStep < dialogs.length) {
                typeWriter(galText, dialogs[currentStep], 40);
            } else {
                document.getElementById('galgame-screen').style.display = 'none';
            }
        }

        function typeWriter(element, text, speed) {
            clearInterval(typingTimer);
            element.innerText = "";
            let index = 0;
            isTyping = true;

            typingTimer = setInterval(() => {
                if (index < text.length) {
                    element.innerText += text.charAt(index);
                    index++;
                    playSFX('typewriter');
                } else {
                    clearInterval(typingTimer);
                    isTyping = false;
                }
            }, speed);
        }

        document.getElementById('galgame-screen').addEventListener('click', () => {
            const galText = document.getElementById('galText');
            playSFX('click');
            if (isTyping) {
                clearInterval(typingTimer);
                galText.innerText = dialogs[currentStep];
                isTyping = false;
            } else {
                currentStep++;
                showDialog();
            }
        });
    </// ==========================================
// 點擊滑鼠特效：產生飄散的小愛心與文字碎屑
// ==========================================
document.addEventListener('click', function(e) {
    // 排除點擊輸入框或特定按鈕時的干擾
    if (e.target.tagName === 'INPUT' || e.target.tagName === 'BUTTON') return;

    // 建立愛心特效元素
    const heart = document.createElement('div');
    heart.innerHTML = '♥';
    heart.style.position = 'fixed';
    heart.style.left = e.clientX + 'px';
    heart.style.top = e.clientY + 'px';
    heart.style.color = Math.random() > 0.5 ? '#FF3385' : '#00FFFF'; // 隨機粉色或霓虹藍
    heart.style.fontSize = '1.2rem';
    heart.style.fontWeight = 'bold';
    heart.style.pointerEvents = 'none';
    heart.style.zIndex = '99999';
    heart.style.userSelect = 'none';
    
    // 動態加入 CSS 動畫效果
    heart.style.transition = 'transform 0.8s ease-out, opacity 0.8s ease-out';
    heart.style.transform = 'translate(-50%, -50%) scale(1)';
    
    document.body.appendChild(heart);

    // 隨機飄散的方向
    const angle = Math.random() * Math.PI * 2;
    const distance = 40 + Math.random() * 40;
    const targetX = Math.cos(angle) * distance;
    const targetY = Math.sin(angle) * distance;

    // 強制觸發重繪後執行動畫
    requestAnimationFrame(() => {
        heart.style.transform = `translate(calc(-50% + ${targetX}px), calc(-50% + ${targetY}px)) scale(0.4) rotate(${Math.random() * 60 - 30}deg)`;
        heart.style.opacity = '0';
    });

    // 動畫結束後自動移除元素
    setTimeout(() => {
        heart.remove();
    }, 800);
});>
</body>
</html>